{
  "name": "linkedin-post-architect",
  "private": true,
  "version": "1.4.1",
  "description": "A vanilla JavaScript LinkedIn post generator.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "npx eslint script.js --no-config-lookup --rule 'no-unused-vars: warn' --rule 'no-undef: error' --global document,window,localStorage,console,setTimeout,Date,exports,module,navigator"
  },
  "devDependencies": {
    "eslint": "^10.2.1",
    "vitest": "^4.1.5"
  }
}
