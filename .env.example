# This is a static HTML/CSS/JS application with no backend dependencies.
# No environment variables are required for local deployment.
#
# For GitHub Pages deployment:
# - Simply push to the main branch
# - GitHub Actions will deploy automatically via .github/workflows/static.yml
#
# No API keys, secrets, or environment variables are needed.
